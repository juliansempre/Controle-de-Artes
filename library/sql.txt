CREATE DATABASE controledeartes;
USE controledeartes;

CREATE TABLE artesaroeira (
id INT AUTO_INCREMENT PRIMARY KEY,
artes VARCHAR(255),
link VARCHAR(255),
data  VARCHAR(20),
status VARCHAR(20),
data_hora TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO artesaroeira (artes, link, data, status, data_hora)
VALUES ('arte 01', 'http://www.google.com', '2023-07-06', 'Fazendo', NOW());


ALTER TABLE artesaroeira
ADD COLUMN data_hora TIMESTAMP DEFAULT CURRENT_TIMESTAMP;


GRANT ALL PRIVILEGES ON controledeartes.* TO 'root'@'%' IDENTIFIED BY '';
FLUSH PRIVILEGES;
